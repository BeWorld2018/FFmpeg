prefix=/usr/local
exec_prefix=${prefix}
libdir=/usr/local/lib
includedir=/usr/local/include

Name: libavdevice
Description: FFmpeg device handling library
Version: 
Requires: libavfilter >= , libswscale >= , libpostproc >= , libavformat >= , libavcodec >= , libswresample >= , libavutil >=
Requires.private: 
Conflicts:
Libs: -L${libdir}  -lavdevice -lm -L/usr/local/lib -noixemul -lSDL2 -lc -lm -lc
Libs.private: 
Cflags: -I${includedir}
